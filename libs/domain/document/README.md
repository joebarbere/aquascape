# `@aquascape/domain/document`

`.aqua` v1 schema, (de)serialization, migrations. Plan §2.7 / Stage 1 F1.3.

- **Tags:** `scope:domain`, `framework:none`.
- **Stage 0 status:** stub. F1.3 imports `aqua-document.ts` from the repo root.
