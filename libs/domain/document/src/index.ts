// Public API for @aquascape/domain/document.
//
// Owns the .aqua v1 schema, (de)serialization, and the Migration chain.
// Plan §2.7 / Stage 1 F1.3.
//
// The canonical type definition currently lives in `aqua-document.ts` at the
// repo root; F1.3 will move it into this lib and add validation + ZIP container.
export {};
