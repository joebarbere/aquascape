# `@aquascape/domain/livestock-behaviors`

Behaviour-parameter types + species presets + `resolveBehavior()` for animated
livestock (Stage 11 F11.2 — schooling + vertical stratification).

- **Tags:** `scope:domain`, `framework:none`, `domain:livestock-behaviors`.
- **Stage 11 F11.2 Wave 1 status:** scaffold only — behaviour param types,
  per-species presets, and the `resolveBehavior(catalogRef)` resolver land in
  Wave 3. The schooling system in `domain-livestock-ecs` reads from this lib at
  spawn time once Wave 3 lands.
