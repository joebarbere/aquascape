# `@aquascape/testing`

Test harnesses, fixtures, builders, mocks. **Tags:** `scope:testing`, `framework:none`. **Stage 0 status:** stub.

TODO(F1.3): host the document round-trip property test that the CI `document-round-trip` job will run.
