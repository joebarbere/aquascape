// Livestock pulse barrel — see `livestock-pulse.actions.ts` for the rationale.
// No reducer, no selectors: this slice is action-only.

export { LivestockPulseActions } from './livestock-pulse.actions';
