# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
tmp/
out/
out-tsc/

# Per-clone macOS dev bundle (cloned Electron.app with patched Info.plist
# for the dock-hover title — see tools/build-dev-bundle.sh).
apps/desktop/.dev-bundle/

# electron-builder cache (downloads platform-specific build tools on
# first run; large + machine-specific).
.cache/electron-builder/

# Nx
.nx/cache
.nx/workspace-data

# Angular CLI / Vite cache (created by `nx serve web`)
.angular/

# Test
coverage/
.jest-cache/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Per-user Claude Code settings
.claude/settings.local.json

# Env
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
Thumbs.db
